/**
 * Represents a Wi-Fi network configuration.
 *
 * @interface ISSID
 * @property {string} ssid - The name of the Wi-Fi network.
 * @property {string} password - The password for the Wi-Fi network.
 */
export interface ISSID {
  ssid: string;
  password: string;
}
